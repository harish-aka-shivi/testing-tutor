// export const INITIAL_STATE = {};

// export const initState = state => ({ ...state });

export const INIT_STATE = 'INIT_STATE';
export const MARK_EXPECT_SOLVED = 'MARK_EXPECT_SOLVED';
export const RESET_EXPECT = 'RESET_EXPECT';

// const reducer = (state, action) => {
//   switch (action.type) {
//     case INIT_STATE:
//       return { ...action.payload };
//     case MARK_EXPECT_SOLVED:
//       // save the data here.
//       return { ...state, ...{ [action.payload.id]: true } };
//     default:
//       return state;
//   }
// };

// export default reducer;
